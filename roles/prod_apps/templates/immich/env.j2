# Core settings
IMMICH_VERSION={{ immich_version }}
NODE_ENV=production

# Database
DB_HOSTNAME=immich_postgres
DB_USERNAME=immich
DB_PASSWORD={{ vault_immich_db_password }}
DB_DATABASE_NAME=immich

# Redis
REDIS_HOSTNAME=immich_redis

# Timezone
TZ={{ timezone }}

# Upload location
UPLOAD_LOCATION={{ mnt_photos }}
